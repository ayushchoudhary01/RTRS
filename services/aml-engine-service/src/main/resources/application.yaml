spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: aml-engine-service

  datasource:
    url: jdbc:postgresql://localhost:5436/aml_db
    username: rtrs_aml
    password: rtrs_aml_pass
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000

  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  kafka:
    bootstrap-servers: localhost:9092,localhost:9093,localhost:9094
    properties:
      schema.registry.url: http://localhost:8081
    consumer:
      group-id: aml-engine-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer

server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  metrics:
    export:
      prometheus:
        enabled: true
  tracing:
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: http://localhost:4318/v1/traces

rtrs:
  security:
    jwt:
      secret-key: ${RTRS_JWT_SECRET}
      issuer: rtrs-auth-service
      audience: rtrs-services
      access-token-expiry-ms: 900000

  kafka:
    topics:
      trade-submitted: trade.submitted.v1
      aml-cleared: aml.cleared.v1
      aml-flagged: aml.flagged.v1

  aml:
    high-value-threshold-usd: 500000
    rapid-succession-window-minutes: 5
    rapid-succession-max-trades: 3
    sanctioned-countries: IR,KP,SY,CU,VE

logging:
  level:
    com.rtrs: INFO
    org.springframework.kafka: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] [traceId=%X{traceId}] %-5level %logger{36} - %msg%n"