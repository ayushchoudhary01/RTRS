spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: trade-processor-service

  datasource:
    url: jdbc:postgresql://localhost:5441/processor_db
    username: rtrs_processor
    password: rtrs_processor_pass
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000

  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  kafka:
    bootstrap-servers: localhost:9092,localhost:9093,localhost:9094
    properties:
      schema.registry.url: http://localhost:8081
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all
      retries: 2147483647
      compression-type: lz4
      properties:
        enable.idempotence: true
        max.in.flight.requests.per.connection: 1
        delivery.timeout.ms: 120000
    consumer:
      group-id: trade-processor-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 1000ms
      lettuce:
        pool:
          max-active: 8
          min-idle: 2

server:
  port: 8087

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  metrics:
    export:
      prometheus:
        enabled: true
  tracing:
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: http://localhost:4318/v1/traces

rtrs:
  security:
    jwt:
      secret-key: ${RTRS_JWT_SECRET}
      issuer: rtrs-auth-service
      audience: rtrs-services
      access-token-expiry-ms: 900000

  idempotency:
    ttl-hours: 24

  outbox:
    poll-interval-ms: 100
    batch-size: 50

  kafka:
    topics:
      trade-submitted: trade.submitted.v1
      risk-approved: risk.approved.v1
      aml-cleared: aml.cleared.v1
      trade-executed: trade.executed.v1
      trade-rejected: trade.rejected.v1

logging:
  level:
    com.rtrs: INFO
    org.springframework.kafka: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] [traceId=%X{traceId}] %-5level %logger{36} - %msg%n"